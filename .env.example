# ==============================================
# COOLIFY PRODUCTION DEPLOYMENT CONFIGURATION
# ==============================================
# For local development, use different values for VITE_API_BASE_URL and CORS_ALLOWED_ORIGINS
# For Coolify deployment, use the values below

# Database Configuration
POSTGRES_DB=notionclone
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_THIS_SECURE_PASSWORD_IN_PRODUCTION
POSTGRES_PORT=5432

# Redis Configuration
REDIS_PORT=6379

# MinIO Configuration (Object Storage)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=CHANGE_THIS_SECURE_MINIO_PASSWORD
MINIO_BUCKET_NAME=notionclone-uploads
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# Application Environment
# Development for local, Production for Coolify
ASPNETCORE_ENVIRONMENT=Production

# JWT Configuration (CRITICAL - Must be at least 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET_KEY=CHANGE_THIS_GENERATE_WITH_OPENSSL_RAND_BASE64_32
JWT_ISSUER=NotionClone
JWT_AUDIENCE=NotionCloneUsers
JWT_EXPIRY_MINUTES=60

# CORS Configuration
# Local: http://localhost:3000
# Coolify: https://notion.dutchcaz.com
CORS_ALLOWED_ORIGINS=https://notion.dutchcaz.com

# Frontend Build Configuration
# Local: http://localhost:8081
# Coolify: https://api.notion.dutchcaz.com
VITE_API_BASE_URL=https://api.notion.dutchcaz.com

# Port Configuration (usually auto-handled by Coolify)
BACKEND_PORT=8081
FRONTEND_PORT=3000
