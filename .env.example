# ==============================================
# Notion Clone - Environment Variables
# ==============================================
# Copy this file to .env and fill in your values
# IMPORTANT: Never commit .env to version control
# ==============================================

# ----------------------------------------------
# Database Configuration (PostgreSQL)
# ----------------------------------------------
POSTGRES_DB=notionclone
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_postgres_password_here
POSTGRES_PORT=5432

# ----------------------------------------------
# Redis Configuration
# ----------------------------------------------
REDIS_PORT=6379
# Note: Redis password is set via redis.conf or docker command
# For production, ensure Redis is password-protected

# ----------------------------------------------
# MinIO Object Storage Configuration
# ----------------------------------------------
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=your_secure_minio_password_here
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_BUCKET_NAME=notionclone-uploads

# ----------------------------------------------
# Backend (.NET) Configuration
# ----------------------------------------------
ASPNETCORE_ENVIRONMENT=Production
BACKEND_PORT=8080

# JWT Authentication (REQUIRED FOR PRODUCTION)
# Generate a secure random string (minimum 32 characters)
# Example: openssl rand -base64 64
JWT_SECRET_KEY=your_very_long_and_secure_random_secret_key_here_minimum_32_characters
JWT_ISSUER=NotionClone
JWT_AUDIENCE=NotionCloneUsers
JWT_EXPIRY_MINUTES=60

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# For production: https://notion.dutchcaz.com
# For development: http://localhost:3000,http://localhost:5173
CORS_ALLOWED_ORIGINS=https://notion.dutchcaz.com

# ----------------------------------------------
# Frontend Configuration
# ----------------------------------------------
FRONTEND_PORT=3000

# ----------------------------------------------
# Coolify-Specific Variables (for VPS deployment)
# ----------------------------------------------
# These are typically set in Coolify's environment variables UI:
# - Domain: notion.dutchcaz.com
# - Let's Encrypt SSL: Enabled
# - Automatic deployments from GitHub: Enabled

# ----------------------------------------------
# Security Notes
# ----------------------------------------------
# 1. JWT_SECRET_KEY: MUST be changed for production!
#    Generate with: openssl rand -base64 64
#
# 2. Database passwords: Use strong, unique passwords (32+ chars)
#    Generate with: openssl rand -base64 32
#
# 3. MinIO credentials: Change from defaults
#    Generate with: openssl rand -base64 32
#
# 4. Enable HTTPS in production (Coolify handles this automatically)
#
# 5. Never expose database/Redis/MinIO ports to the internet
#    Only expose backend (8080) and frontend (3000) through reverse proxy
#
# 6. For production, set ASPNETCORE_ENVIRONMENT=Production
#
# 7. Ensure all secrets are stored securely in Coolify's environment
#    variables, not in the codebase
